{
  "name": "npm-update-scanner",
  "version": "1.0.0",
  "description": "Scans NPM dependencies to get their version-to-version diffs",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && dtsm install",
    "compile": "tsc --project .",
    "compile_test": "tsc --project ./test --pretty",
    "test": "mocha --opts test/support/default.opts build/test"
  },
  "keywords": [
    "npm",
    "update",
    "github",
    "ci"
  ],
  "author": "FUJI Goro",
  "license": "Apache-2.0",
  "engines": {
    "node": ">= 4.2.0"
  },
  "dependencies": {
    "semver": "^5.3.0",
    "request": "^2.73.0"
  },
  "devDependencies": {
    "typescript": ">= 2.0.0",
    "typescript-formatter": "^2.3.0",
    "dtsm": "^1.1.0",
    "mocha": "^2.5.1",
    "nexpect": "^0.5.0",
    "power-assert": "^1.4.1",
    "source-map-support": "^0.4.0"
  }
}