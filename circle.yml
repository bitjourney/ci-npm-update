machine:
  node:
    version: 6

dependencies:
  pre:
    - npm run setup
    - npm run build

deployment:
  update-dependencies:
    branch: master
    commands:
      - >
        if [ -n "${NPM_UPDATE}" ] ; then
          bin/ci-npm-update
        fi
